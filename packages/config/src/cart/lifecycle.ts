/**
 * Number of days a cart item remains reserved before backend cleanup removes it.
 * This value is shared by backend expiry logic and client presentation copy.
 */
export const CART_ITEM_TTL_DAYS = 3;
