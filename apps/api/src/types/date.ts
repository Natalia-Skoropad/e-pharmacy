/** Backend-local semantic primitive for a full ISO-8601 timestamp. */
export type ISODateTimeString = string;

/** Backend-local semantic primitive for a YYYY-MM-DD calendar date. */
export type CalendarDateString = string;
